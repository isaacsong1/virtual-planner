import React, { useState } from "react";

const Profile = () => {
  const [isEditting, setIsEditting] = useState(false)

  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
};

export default Profile;
